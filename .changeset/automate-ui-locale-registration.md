---
'@youversion/platform-react-ui': patch
---

Automate UI locale registration from locale JSON files via `generate:i18n`, and run generation before build, typecheck, Storybook, and test commands so new languages stay in sync without hand-editing the resource map.
