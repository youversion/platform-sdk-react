---
'@youversion/platform-react-ui': minor
---

Add optional controlled-mode `highlights` prop to `BibleTextView`, `BibleCard`, and `VerseOfTheDay` so hosts can render existing highlights without a fetch or verse-action UI.
