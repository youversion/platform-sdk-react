---
'@youversion/platform-core': patch
'@youversion/platform-react-hooks': patch
'@youversion/platform-react-ui': patch
---

Split the UI JavaScript build so a `YouVersionProvider`-only import can drop `BibleReader`, `BibleChapterPicker`, and `BibleVersionPicker`. Import zod Mini as a namespace so unused classic locale files and unused Mini helpers stay out of named-import graphs. Keep core as a runtime dependency so UI and hooks share one copy. Re-export core values from UI by name, not `export *`. Provider records locale without importing i18next or locale JSON.

The package root is still the only public entry. The three Bible components are separate files in `dist`. English is the only eager locale. Other locale files load on demand. Reader CSS loads with `BibleTextView`, not `YouVersionProvider`. Provider embeds chrome CSS for the missing-app-key panel only. Scripture, pickers, auth, the verse popover, ProfileAvatar, Separator, and Textarea embed the full utility sheet. The `X-YVP-Sdk` stamp lives in published core. Generated i18n types stay a catalog map so published `.d.ts` files do not inline every locale JSON shape. Embedded reader CSS is not a second public stylesheet file. Core and hooks wipe `dist` on build so stale tsup `.d.cts` files cannot publish. The size-limit gate keeps esbuild splitting off so locale `import()`s stay inlined. Published JS is whitespace-minified; syntax and identifier minify stay off so the `X-YVP-Sdk` stamp remains verifiable. The version string is inlined from `package.json` without embedding the rest of the manifest. Provider chrome embeds the scoped reset plus missing-app-key tokens, not the full brand palette. Core clients are separate tsup entries so an ApiClient or Provider import can drop BibleClient. Hooks import `YouVersionContext` from its module, not the context barrel, so `useChapter` does not evaluate Auth. ProfileAvatar imports `@radix-ui/react-avatar` instead of the `radix-ui` umbrella. Hooks and UI run tsup's Rollup tree-shake pass; core does not, because that pass folds `isPublishBuild` and trips the stamp guard. Chrome, component, and reader style injectors are separate modules. Generated locale catalogs drop unused keys; Crowdin source files stay complete. Bible version-filter allowlists live in a small state module so `useChapter` can read them without pulling auth storage or grants. `YouVersionPlatformConfiguration` still exposes the same statics. Unused Item and InputGroup variants are gone. Chart and sidebar Tailwind theme keys are gone. Public `theme.css` keeps the `--yv-*` tokens SDK components read, not unused Figma steps.
