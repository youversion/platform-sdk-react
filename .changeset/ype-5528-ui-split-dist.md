---
'@youversion/platform-react-ui': patch
---

Split the UI JavaScript build so a `YouVersionProvider`-only import can drop `BibleReader`, `BibleChapterPicker`, and `BibleVersionPicker`. Keep core as a runtime dependency so UI and hooks share one copy. Re-export core values from UI by name, not `export *`. Provider records locale without importing i18next or locale JSON.

The package root is still the only public entry. The three Bible components are separate files in `dist`. English is the only eager locale. Other locale files load on demand. Reader CSS loads with `BibleTextView`, not `YouVersionProvider`. Provider embeds chrome CSS for the missing-app-key panel only. Scripture, pickers, auth, the verse popover, ProfileAvatar, Separator, and Textarea embed the full utility sheet. Generated i18n types stay a catalog map so published `.d.ts` files do not inline every locale JSON shape. Embedded reader CSS is not a second public stylesheet file. The UI build starts from an empty `dist` so stale hashed chunks cannot publish. The size-limit gate keeps esbuild splitting off so locale `import()`s stay inlined. ProfileAvatar imports `@radix-ui/react-avatar` instead of the `radix-ui` umbrella. UI runs tsup's Rollup tree-shake pass. Chrome, component, and reader style injectors are separate modules. Generated locale catalogs drop unused keys; Crowdin source files stay complete. Unused Item and InputGroup variants are gone. Chart and sidebar Tailwind theme keys are gone. `useChapter` reads version-filter allowlists without pulling auth storage or grants. `YouVersionPlatformConfiguration` still exposes the same statics.
